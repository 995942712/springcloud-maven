# sleuth 配置
# 配置服务应用名称
spring:
  application:
    name: sleuth
  cloud:
    config:
      name: ${spring.application.name}
      uri: http://admin:123456@localhost:8769
#      fail-fast: true









