# provider 配置
# 配置服务应用名称
spring:
  application:
    name: provider
  cloud:
    config:
      name: ${spring.application.name}
      uri: http://admin:123456@localhost:8769
#      fail-fast: true









